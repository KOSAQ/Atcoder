#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define rep2(i, s, n) for (int i = (s); i <= (int)(n); i++)

int main(void){
    //input-----
    int N, M;
    cin >> N >> M;
    char S[209][209];
    rep2(i, 1, N){
        rep2(j, 1, M){
            cin >> S[i][j];
        }
    }
    
    //----------

    return 0;
}